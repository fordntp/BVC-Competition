-- =====================================================================
-- ย่อ token ของลิงก์คำเชิญจาก 32 ตัวเหลือ 8 ตัว
-- รันหลัง shared-link.sql — รันซ้ำได้ ไม่พัง
--
-- สเปคเดิมใช้ 32 ตัว เพราะลิงก์เป็นความลับรายคน เดาถูกคือสวมสิทธิ์คนอื่น
-- แบบใหม่แจกลิงก์เดียวทั้งคลับ ความลับของ token แทบไม่เหลือความหมาย
-- ตัวกันจริงคือ เวลาเปิดประตู + ต้องมีบัญชี Google + หนึ่งบัญชีหนึ่งที่นั่ง
--
-- 8 ตัวอักษรฐานสิบหก = 4,294,967,296 แบบ ต่อให้ยิงเดา 100 ครั้งต่อวินาที
-- ก็ใช้เวลาเฉลี่ยราวหนึ่งปีครึ่ง และ Supabase จะกันไปก่อนนานแล้ว
-- =====================================================================

alter table invites
  alter column token set default encode(gen_random_bytes(4), 'hex');
